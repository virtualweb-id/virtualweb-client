const initialState = {
  invitation: {},
  isLoading: false,
  isError: '',
}

export const invitationReducer = (state = initialState, action) => {
  const { type, payload } = action
  switch (type) {
    default:
      return state
  }
}